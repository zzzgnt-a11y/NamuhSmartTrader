/* Final trade-history owner: KR/US separated, no buy/sell filter controls. */
(()=>{
'use strict';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]||c));
const won=n=>Math.round(Number(n||0)).toLocaleString('ko-KR')+'원';
const pct=n=>(Number(n||0)>=0?'+':'')+Number(n||0).toFixed(2)+'%';
const qty=n=>Math.max(0,Number(n||0)).toLocaleString('ko-KR',{maximumFractionDigits:8})+'주';
function market(){return document.getElementById('usModeLabel')?.classList.contains('active')?'US':'KR'}
function strategy(t){const s=String(t?.strategy_display||t?.strategy||'').trim();return s==='SCALP'?'조건1':s}
let latest={KR:[],US:[]},lastKey='';
function price(v,m){return m==='US'?'$'+Number(v||0).toLocaleString('en-US',{maximumFractionDigits:4}):Math.round(Number(v||0)).toLocaleString('ko-KR')+'원'}
function ensure(){
  let sec=document.getElementById('tradeSec');const profit=document.getElementById('profitSec');
  if(!sec){const holding=document.getElementById('holdingSec');if(!holding||!profit)return null;sec=document.createElement('section');sec.id='tradeSec';sec.className='section-shell';sec.innerHTML='<div class="section-head"><div><span class="section-index">05</span><div><small>TRADE HISTORY</small><h2>거래내역</h2></div></div><b id="tradeScopeLabel"></b></div><div id="stockTrades" class="stock-trade-list"></div>';profit.parentNode.insertBefore(sec,profit);const idx=profit.querySelector('.section-index');if(idx)idx.textContent='06';}
  if(!document.getElementById('stockTradeStyle')){const style=document.createElement('style');style.id='stockTradeStyle';style.textContent=`.stock-trade-list{display:grid;gap:8px}.stock-trade-row{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:13px 14px;border-radius:16px;background:rgba(255,255,255,.62);border:1px solid rgba(37,73,124,.12)}.stock-trade-row>div{min-width:0}.stock-trade-row>div:last-child{text-align:right;flex:0 0 auto}.stock-trade-row b{font-size:11px}.stock-trade-row small{display:block;margin-top:4px;font-size:8px;color:#70809b;line-height:1.45}.trade-side{display:inline-flex;align-items:center;justify-content:center;min-width:38px;margin-right:7px;padding:4px 6px;border-radius:8px;font-size:8px;font-weight:900}.trade-side.buy{background:rgba(0,152,108,.09);color:#00986c}.trade-side.sell{background:rgba(224,72,100,.09);color:#e04864}`;document.head.appendChild(style);}
  return sec;
}
function render(){ensure();const m=market(),rows=(latest[m]||[]).filter(t=>String(t.market||m).toUpperCase()===m).slice(0,80),box=document.getElementById('stockTrades');if(!box)return;const label=document.getElementById('tradeScopeLabel');if(label)label.textContent=m==='US'?'미장 모의매매 내역':'국장 모의매매 내역';document.querySelectorAll('[data-trade-filter],.trade-filter,.trade-filters,#stockTradeFilters,#coinTradeFilters').forEach(x=>x.remove());const key=m+'|'+rows.map(t=>[t.side,t.code,t.qty,t.price,t.date,t.time,t.strategy,t.reason,t.pnl,t.pnl_pct].join(':')).join('|');if(key===lastKey&&box.dataset.rendered==='1')return;lastKey=key;box.dataset.rendered='1';box.innerHTML=rows.length?rows.map(t=>{const side=String(t.side||'').toUpperCase(),st=strategy(t),reason=String(t.reason||'').trim(),detail=[st,reason&&reason!==st?reason:''].filter(Boolean).join(' · '),pnl=Number(t.pnl||0),pp=Number(t.pnl_pct||0),unit=side==='BUY'?'매수가':'매도가',amount=side==='SELL'?`${won(pnl)} · ${pct(pp)}`:`매수금액 ${won(t.gross_krw)}`;return `<div class="stock-trade-row"><div><span class="trade-side ${side==='BUY'?'buy':'sell'}">${esc(side)}</span><b>${esc(t.name||t.code)}</b><small>${esc(t.date||'')} ${esc(t.time||'')}${detail?' · '+esc(detail):''}</small></div><div><b>${unit} ${price(t.price,m)} · ${qty(t.qty)}</b><small class="${pnl>=0?'pos':'neg'}">${amount}</small></div></div>`}).join(''):`<div class="empty">아직 ${m==='US'?'미장':'국장'} 모의매매 내역이 없습니다.</div>`;}
if(!window.__NAMUH_TRADE_FETCH_WRAPPED__){window.__NAMUH_TRADE_FETCH_WRAPPED__=true;const nativeFetch=window.fetch.bind(window);window.fetch=async(...args)=>{const res=await nativeFetch(...args);try{const u=String(args?.[0] instanceof Request?args[0].url:args?.[0]||'');if(u.includes('/api/state'))res.clone().json().then(d=>{const m=String(d?.mode||'').toUpperCase();if(m==='KR'||m==='US'){latest[m]=d?.paper?.trades||[];requestAnimationFrame(render)}}).catch(()=>{})}catch(_){}return res};}
function init(){ensure();render();document.getElementById('krModeLabel')?.addEventListener('click',()=>requestAnimationFrame(render));document.getElementById('usModeLabel')?.addEventListener('click',()=>requestAnimationFrame(render))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
